services:
  - type: web
    name: candidate-search-app
    env: node
    buildCommand: "npm run build"
    startCommand: "npm run start"
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
